pipeline {
    agent any

    stages {
        stage ('Audit tools') {
            steps {
                sh '''
                    java -version
                    gradle -v
                '''
            }
        }
        stage ('Unit Test') {
            steps {
                sh 'gradlew test'
            }
        }
    }
}
